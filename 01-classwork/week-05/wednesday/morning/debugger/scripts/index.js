const fruits = ['oranges', 'bananas', 'pears', 'tomato'];

fruits.forEach((fruit) => {
  // debugger;
  console.log(fruit);
});

const p = document.createElement('p');
p.innerText = 'Hola';

document.body.appendChild(p);

window.addEventListener('keypress', (event) => {
  debugger;
  console.log('cualquier cosa');
  const hola = 'hola';
  debugger;
});
